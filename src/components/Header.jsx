import React from "react";

const Header = () => (
  <header className="w-full bg-[#8000ff] text-white py-4 px-6 flex items-center justify-between shadow-md">
    <div className="text-xl font-bold tracking-wide">Aviator Game</div>
    {/* Add more header content here if needed, e.g., user info, logo, etc. */}
  </header>
);

export default Header;
